<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1" charset="UTF-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <title>{% block title %}The Daily Grunt{% endblock %}</title>
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}"/>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}"/>

    <!-- [if It IE9]>
    <script src="https://oss.maxcdn.com/html5hiv/3.7.2/html5.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
{% block navbar %}
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation" >
        <div class="center">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-container">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path ('grunt_homepage') }}">
                    <img src="{{ asset ('images/logo.png') }}" />
                </a>
            </div>
            <div class="çolapse nabar-collapse" id="navbar-container">
                <ul class="nav navbar-nav">
                    <li{% if app.request.attributes.get('_route') == 'grunt_homepage'%} class = "active" {% endif%}><a href="{{ path ('grunt_homepage') }}"><span class="glyphicon glyphicon-home"></span> Home</a></li>
                    <li{% if app.request.attributes.get('_route') == 'grunt_garaj'%} class = "active" {% endif%}><a href="{{ path ('grunt_garaj') }}"><span class="glyphicon glyphicon-wrench"></span> Garaj</a></li>
                    <li{% if app.request.attributes.get('_route') == 'grunt_jurnal'%} class = "active" {% endif%}><a href="{{ path ('grunt_jurnal') }}"><span class="glyphicon glyphicon-road"></span> Jurnal de Bord</a></li>
                    <li{% if app.request.attributes.get('_route') == 'grunt_diy'%} class = "active" {% endif%}><a href="{{ path ('grunt_diy') }}"><span class="glyphicon glyphicon-scissors"></span> Proiecte DYI</a></li>
                    <li{% if app.request.attributes.get('_route') == 'grunt_contact'%} class = "active" {% endif%}><a href="{{ path ('grunt_contact') }}"><span class="glyphicon glyphicon-envelope"></span> Contact</a></li>
                    <li>
                        {% if is_granted('ROLE_USER') %}
                            <a href="{{ path('security_logout') }}"><span class="glyphicon glyphicon-user"></span> {{ app.user.username  }} <span class="glyphicon glyphicon-off"></span> Logout </a>
                        {% else %}
                            <form class="navbar-form navbar-right" role="form" method="post" id="loginform" name="loginform" enctype="multipart/form-data" action="{{ path('security_login') }}" >
                                <div class="input-group">
									<span class="input-group-addon">
									<span class="glyphicon glyphicon-user"></span>
									</span>
                                    <input type="text" class="form-control" {{ last_username is defined ? ' value=' ~ last_username ~ ' ' : '' }} name="_username" id="_username" placeholder="Utilizator" />
                                </div>
                                <div class="input-group">
									<span class="input-group-addon">
									<span class="glyphicon glyphicon-lock"></span>
									</span>
                                    <input type="password" class="form-control" name="_password" id="_password" placeholder="Parola" />
                                </div>
                                <button type="submit" class="btn bnt-info">Autentifica</button>
                            </form>
                        {%  endif %}
                    </li>
                </ul>
                {% if app.request.attributes.get('_route') == 'security_login'  %}
                    {% if error %}
                        <div class="bad_cred">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}
                {% endif %}
            </div>
        </div>
    </nav>
{% endblock %}
{% block body %}
    {% block jumbo %}
<div class="container">

    <div class="jumbotron">
        <h1>The Daily Grunt</h1>
        {% if is_granted('ROLE_USER') %}
            <p class="text-right"><em>...noah bine ca esti  <strong>autentificat</strong> amu poti <strong> comenta </strong> :) </em></p>
        {%  else %}
            <p class="text-right"><em>...daca vrei sa <strong>comentezi</strong> e musai sa te <strong><a href="{{ path ('grunt_register') }}">inregistrezi</a></strong> :) </em></p>
        {% endif %}
    </div>
    {% endblock jumbo %}
{% endblock %}
{% block javascripts %}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="{{ asset ('js/bootstrap.min.js') }}"></script>
{% endblock %}
</body>
</html>