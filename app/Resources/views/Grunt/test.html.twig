{% extends '::base.html.twig' %}

{% block body %}
    {% for article in articles %}

        <div class="col-xs-2">
            <table class="table table-responsive table-hover">
                <tr>
                    <th>Picture Name</th>
                    <th>Picture Name</th>
                </tr>
                <tr>
                    <td>
                        <div class="img-responsive">
                        <img src="{{ asset ('upload/' ~ article.Picture) }}" /></td>
                        </div>
                    <td>{{ 'upload/' ~ article.Picture }}</td>
                </tr>
            </table>
        </div>

    {% endfor %}
    <div class="row">
        <div class="col-xs-12">
            {{ form_start(CKeFrom) }}
                {{ form_widget(CKeFrom) }}
            <button class="btn btn-info">Update</button>
            {{ form_end(CKeFrom) }}
        </div>
    </div>
{% endblock %}



